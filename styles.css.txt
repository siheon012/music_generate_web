@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components {
  .chat-container {
    @apply max-w-3xl mx-auto bg-white rounded-lg shadow-md overflow-hidden;
  }

  .chat-header {
    @apply bg-gray-800 text-white p-4 text-xl font-bold;
  }

  .chat-messages {
    @apply p-4 h-[calc(100vh-200px)] overflow-y-auto;
  }

  .message {
    @apply mb-4 last:mb-0;
  }

  .message-user {
    @apply bg-blue-100 text-blue-800 p-3 rounded-lg max-w-[80%] ml-auto;
  }

  .message-ai {
    @apply bg-gray-100 text-gray-800 p-3 rounded-lg max-w-[80%];
  }

  .prompt-input {
    @apply bg-white border-t border-gray-200 p-4;
  }

  .prompt-input input {
    @apply w-full p-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500;
  }

  .prompt-input button {
    @apply mt-2 w-full bg-blue-500 text-white p-2 rounded-md hover:bg-blue-600 transition-colors;
  }

  .navbar {
    @apply bg-gray-800 text-white p-4;
  }

  .navbar-content {
    @apply max-w-6xl mx-auto flex justify-between items-center;
  }

  .navbar-logo {
    @apply text-2xl font-bold;
  }

  .navbar-links a {
    @apply text-white mx-2 hover:text-blue-300 transition-colors;
  }

  .page-container {
    @apply max-w-6xl mx-auto p-4;
  }

  .form-container {
    @apply max-w-md mx-auto bg-white p-8 rounded-lg shadow-md;
  }

  .form-input {
    @apply w-full p-2 mb-4 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500;
  }

  .form-button {
    @apply w-full bg-blue-500 text-white p-2 rounded-md hover:bg-blue-600 transition-colors;
  }

  .music-list {
    @apply bg-white rounded-lg shadow-md p-4;
  }

  .music-item {
    @apply mb-2 p-2 hover:bg-gray-100 rounded-md transition-colors;
  }
}